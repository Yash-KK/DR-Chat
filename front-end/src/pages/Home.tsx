import Box from "@mui/material/Box";

const Home = () => {
  return (
    <Box component="section" sx={{ p: 2, border: "1px dashed grey" }}>
      This Box renders as an HTML section element on Home.
    </Box>
  );
};

export default Home;
